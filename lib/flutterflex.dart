import 'package:flutter/material.dart';

class abc extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blue,
        child: Column(children: [Text("rdyas")]),



      ),
    );
  }
}
